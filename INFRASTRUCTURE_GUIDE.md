# 📋 Повний опис інфраструктури проекту

> **Комплексний гід по AWS інфраструктурі з Terraform, Docker та GitLab CI/CD**
> 
> Останнє оновлення: Грудень 2024 | Версія: 2.0 | 1400+ рядків документації

---

## 📚 Зміст

1. [🎯 Загальна мета проекту](#-загальна-мета-проекту)
2. [📁 Структура проекту та опис файлів](#-структура-проекту-та-опис-файлів)
   - [Кореневі файли](#-кореневі-файли)
   - [Папка infra/](#-папка-infra---основна-інфраструктура)
   - [Папка infra/app/](#-папка-infraapp---застосунок)
   - [Папка setup/](#-папка-setup---початкове-налаштування)
3. [🔄 Як все працює разом](#-як-все-працює-разом)
   - [Передумови](#-передумови-що-потрібно-зробити-вручну)
   - [Початкове розгортання](#1️⃣-початкове-розгортання-перший-раз)
   - [CI/CD Pipeline](#2️⃣-cicd-pipeline-автоматичний-деплой-при-кожному-push)
   - [Доступ до застосунку](#3️⃣-доступ-до-застосунку)
   - [Оновлення застосунку](#4️⃣-оновлення-застосунку-після-змін-коду)
   - [Моніторинг та дебаг](#5️⃣-моніторинг-та-дебаг)
4. [🎯 Архітектура](#-архітектура-схема)
5. [🚀 Команди для роботи](#-команди-для-роботи)
6. [💰 Приблизна вартість](#-приблизна-вартість)
7. [🔐 Безпека](#-безпека)
8. [📝 Чекліст для деплою](#-чекліст-для-деплою)
9. [🆘 Troubleshooting](#-troubleshooting)
10. [❓ FAQ](#-faq-часті-питання)
11. [📖 Глосарій термінів](#-глосарій-термінів-та-абревіатур)
12. [📚 Додаткові ресурси](#-додаткові-ресурси)
13. [🎓 Корисні команди](#-корисні-команди)
14. [✅ Production Checklist](#-checklist-готовність-до-production)

---

## 🎯 Загальна мета проекту

Цей проект автоматизує розгортання Python застосунку (Streamlit) у AWS за допомогою:
- **Terraform** - для створення інфраструктури (Infrastructure as Code)
- **GitLab CI/CD** - для автоматичного деплою (continuous deployment)
- **Docker** - для контейнеризації застосунку (portable environments)
- **AWS ECR** - для зберігання Docker-образів (private registry)
- **AWS Systems Manager (SSM)** - для управління EC2 без SSH (secure access)

**Ключові переваги цього підходу:**
- ✅ Повна автоматизація від коду до production
- ✅ Інфраструктура як код (можна відтворити за хвилини)
- ✅ Безпечний доступ до серверів без SSH ключів
- ✅ Ізоляція компонентів (EC2 в приватних мережах)
- ✅ Автоматичне масштабування (готовий до зростання)
- ✅ Zero-downtime deployments (з Instance Refresh)

**Для кого цей гід:**
- DevOps інженерів (повне розуміння інфраструктури)
- Розробників (як деплоїти і дебажити)
- Менеджерів проектів (розуміння архітектури та витрат)

---

## 📁 Структура проекту та опис файлів

### 🔧 Кореневі файли

#### `.gitlab-ci.yml`
**Що робить:**
- Автоматично запускає Terraform при push у GitLab
- Будує Docker-образ з вашого застосунку
- Завантажує образ в AWS ECR
- Деплоїть застосунок на EC2 через SSM

**Що створюється в AWS:**
- Нічого напряму, але запускає процеси:
  - Terraform apply (створює всю інфру)
  - Docker build & push (образи в ECR)
  - SSM commands (запуск контейнера на EC2)

**Стадії pipeline:**
1. `terraform_plan` - валідація Terraform
2. `terraform_apply` - застосування інфраструктури
3. `pre_build` - перевірка ECR
4. `build_and_push` - білд і push Docker-образу
5. `deploy` - деплой через SSM

#### `new.js`
**Що робить:**
- JSON конфігурація для OIDC Trust Policy
- Визначає умови для GitLab федерації з AWS

**Що створюється в AWS:** Нічого (допоміжний файл для налаштування OIDC)

#### `README.md`
**Що робить:**
- Документація проекту
- Інструкції для деплою
- Розшифровка абревіатур (VPC, ALB, ASG, ECR, SSM)

**Що створюється в AWS:** Нічого (це просто документація)

#### `s3.tf`
**Що робить:**
- Створює S3-бакет для зберігання файлів (загального призначення)
- ⚠️ **Увага:** Це окремий бакет, не той що використовується для Terraform backend!

**Що створюється в AWS:**
- ☁️ **S3 Bucket**: `newbucktest123456`
- Використання: зберігання артефактів, логів або резервних копій
- Тег: Environment = dev

#### `terraform.tfstate` та `terraform.tfstate.backup`
**Що робить:**
- Зберігає поточний стан інфраструктури
- Terraform використовує це для відстеження змін
- ⚠️ **Важливо:** Ці файли мають бути тільки в корені проекту, не в папці `infra/`

**Що створюється в AWS:** Нічого (локальні файли стану)

**Чому два файли:**
- `terraform.tfstate` - поточний стан
- `terraform.tfstate.backup` - резервна копія попереднього стану

---

## 📂 Папка `infra/` - Основна інфраструктура

> **📌 Примітка:** Всі файли нижче знаходяться в папці `infra/` та керують основною інфраструктурою застосунку.

### `backends.tf`
**Що робить:**
- Налаштовує віддалене зберігання стану Terraform в S3
- Використовує DynamoDB для блокування стану (щоб кілька людей не змінювали одночасно)
- ⚠️ **Важливо:** Бакет `bucket123ultra` для backend - це НЕ той самий бакет що в `s3.tf`!

**Що створюється в AWS:**
- 📦 **S3 Backend**: зберігає `terraform.tfstate` в бакеті `bucket123ultra`
  - Цей бакет має бути створений **вручну ДО** запуску Terraform
  - Використовується для зберігання стану інфраструктури
- 🔒 **DynamoDB Table**: `terraform-lock-table` для state locking
  - Також має бути створена **вручну ДО** запуску Terraform
  - Запобігає одночасним змінам від різних користувачів

**Два різних S3 бакети в проекті:**
1. `bucket123ultra` (backends.tf) - для Terraform state
2. `newbucktest123456` (s3.tf) - для зберігання файлів застосунку

---

### `providers.tf`
**Що робить:**
- Підключає AWS провайдер для Terraform
- Встановлює регіон `us-east-1`

**Що створюється в AWS:** Нічого (це тільки конфігурація)

---

### `variables.tf`
**Що робить:**
- Визначає змінні які можна використовувати в інших файлах:
  - `aws_region` - регіон AWS
  - `instance_type` - тип EC2 (t2.micro)
  - `key_pair_name` - SSH ключ (опціонально)
  - `db_name`, `db_username`, `db_password` - параметри БД

**Що створюється в AWS:** Нічого (це тільки визначення змінних)

---

### `terraform.tfvars`
**Що робить:**
- Присвоює конкретні значення змінним з `variables.tf`

**Що створюється в AWS:** Нічого (це значення для змінних)

---

### `vpc.tf`
**Що робить:**
- Створює віртуальну приватну мережу для ізоляції ресурсів
- Створює публічні та приватні підмережі
- Налаштовує маршрутизацію

**Що створюється в AWS:**
- 🌐 **VPC**: `main-vpc` (10.0.0.0/16)
- 🌍 **Internet Gateway**: для доступу до інтернету
- 📡 **Public Subnets** (2 шт): для ALB (10.0.1.0/24, 10.0.2.0/24)
- 🔒 **Private Subnets** (2 шт): для EC2 (10.0.10.0/24, 10.0.11.0/24)
- 🚪 **NAT Gateways** (2 шт): для вихідного трафіку з приватних підмереж
- 🗺️ **Route Tables**: для маршрутизації трафіку

**Схема:**
```
Internet
    ↓
Internet Gateway
    ↓
Public Subnets (ALB тут)
    ↓
NAT Gateway
    ↓
Private Subnets (EC2 тут)
```

---

### `sg.tf`
**Що робить:**
- Створює Security Groups (віртуальні фаєрволи)
- Контролює який трафік дозволений

**Що створюється в AWS:**
- 🛡️ **ALB Security Group**:
  - Вхідний: порт 80 (HTTP), порт 8501 з інтернету
  - Вихідний: всі порти

- 🛡️ **EC2 Security Group**:
  - Вхідний: порт 8501 (тільки від ALB), SSH порт 22 (тільки з VPC)
  - Вихідний: всі порти

- 🛡️ **RDS Security Group**:
  - Вхідний: порт 3306 MySQL (тільки від EC2)
  - Вихідний: немає

**Потік трафіку:**
```
Internet → ALB (80, 8501) → EC2 (8501) → RDS (3306)
```

---

### `alb.tf`
**Що робить:**
- Створює Application Load Balancer для розподілу трафіку
- Створює Target Group для групування EC2 інстансів
- Налаштовує Health Check для перевірки здоров'я інстансів

**Що створюється в AWS:**
- ⚖️ **Application Load Balancer**: `backend-alb`
  - Публічний DNS для доступу до застосунку
  - Розподіляє трафік між EC2 інстансами

- 🎯 **Target Group**: `backend-tg`
  - Групує EC2 інстанси
  - Health Check: перевіряє `/` кожні 30 сек

- 👂 **Listener**: слухає на порту 80 і перенаправляє на Target Group

**Як працює:**
```
Користувач → http://backend-alb-xxx.us-east-1.elb.amazonaws.com
    ↓
ALB розподіляє трафік
    ↓
EC2 інстанс (порт 8501)
```

---

### `iam.tf`
**Що робить:**
- Створює IAM роль для EC2 з дозволами
- Дає EC2 доступ до AWS сервісів

**Що створюється в AWS:**
- 👤 **IAM Role**: `ec2-ssm-role`
  - Дозволяє EC2 використовувати SSM (віддалене управління)
  - Дозволяє читати з S3
  - Дозволяє завантажувати образи з ECR
  - Дозволяє відправляти логи в CloudWatch

- 📋 **Instance Profile**: `ec2-ssm-profile`
  - Прив'язує роль до EC2

**Дозволи:**
- ✅ SSM - віддалене управління без SSH
- ✅ S3 - завантаження файлів
- ✅ ECR - завантаження Docker-образів
- ✅ CloudWatch - відправка логів

---

### `asg_be.tf`
**Що робить:**
- Створює шаблон для запуску EC2 інстансів
- Створює Auto Scaling Group для автоматичного масштабування
- Додає user-data скрипт для налаштування інстансів при запуску

**Що створюється в AWS:**
- 🚀 **Launch Template**: `backend-template`
  - AMI: Amazon Linux 2
  - Тип: t2.micro
  - User Data: скрипт що встановлює Docker, завантажує образ з ECR, запускає контейнер

- 📈 **Auto Scaling Group**: `backend-asg`
  - Min/Max/Desired: 1 інстанс
  - Приватні підмережі
  - Автоматично реєструє в Target Group
  - Health Check: ELB (30 сек grace period)

**User-data скрипт робить:**
1. Оновлює систему
2. Встановлює Docker
3. Встановлює SSM Agent
4. Логіниться в ECR
5. Завантажує Docker-образ
6. Запускає контейнер на порту 8501

---

### `ecr.tf`
**Що робить:**
- Створює приватний Docker Registry в AWS

**Що створюється в AWS:**
- 🐳 **ECR Repository**: `my-app-ecr`
  - Зберігає Docker-образи
  - Автоматичне сканування на вразливості
  - `force_delete = true` - автоматично видаляє образи при destroy

**Як використовується:**
```bash
# CI/CD пушить образи:
docker push 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest

# EC2 завантажує образи:
docker pull 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest
```

---

### `ssm.tf`
**Що робить:**
- Створює SSM Document для автоматизації
- Створює SSM Association для прив'язки документа до EC2

**Що створюється в AWS:**
- 📄 **SSM Document**: `RunTitleCheckerDocker`
  - Містить інструкції для деплою контейнера
  - Параметр: `imagetag` (який тег образу використовувати)

- 🔗 **SSM Association**: прив'язує документ до інстансів з тегом `backend-asg-instance`

**Що робить документ:**
1. Оновлює систему
2. Встановлює Docker (якщо немає)
3. Логіниться в ECR
4. Завантажує образ за тегом
5. Зупиняє старий контейнер
6. Запускає новий контейнер

**Використання:**
```bash
# Вручну через CLI:
aws ssm send-command \
  --instance-ids "i-xxx" \
  --document-name "RunTitleCheckerDocker" \
  --parameters imagetag=latest

# Або автоматично через GitLab CI/CD
```

---

### `rds.tf`
**Що робить:**
- Створює MySQL базу даних

**Що створюється в AWS:**
- 🗄️ **RDS Instance**:
  - Engine: MySQL 8.0
  - Тип: db.t3.micro
  - Приватні підмережі (недоступна з інтернету)
  - Доступна тільки для EC2 інстансів

---

### `s3_upload.tf`
**Що робить:**
- Завантажує файл `title_checker.py` в S3

**Що створюється в AWS:**
- 📤 **S3 Object**: `title_checker.py` в бакеті `bucket123ultra`

---

### `outputs.tf`
**Що робить:**
- Виводить важливу інформацію після створення інфраструктури

**Що виводить:**
- 🌐 Load Balancer DNS (URL для доступу до застосунку)
- 🆔 VPC ID
- 🆔 Subnet IDs
- 🆔 Security Group IDs
- 🆔 Auto Scaling Group name

**Приклад виводу:**
```
load_balancer_url = "http://backend-alb-1234567890.us-east-1.elb.amazonaws.com"
vpc_id = "vpc-0abcd1234"
```

---

---

### `requirements.txt`
**Що робить:**
- Список Python бібліотек для Terraform AWS Provider
- ⚠️ **Увага:** Це не для Python застосунку! Для застосунку немає окремого файлу requirements.txt

**Зміст:** Файл може бути порожнім або містити залежності для Terraform

---

## 📂 Папка `infra/app/` - Застосунок

### `Dockerfile`
**Що робить:**
- Описує як зібрати Docker-образ для застосунку
- Встановлює залежності Python (Streamlit, pandas, boto3, mysql-connector-python)
- Копіює код застосунку
- Відкриває порт 8501

**Що створюється:**
- 🐳 Docker-образ з Python Streamlit застосунком

**Базовий образ:** `python:3.9-slim`

**Команда запуску:**
```dockerfile
CMD ["streamlit", "run", "title_checker.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

---

### `title_checker.py`
**Що робить:**
- Код вашого Python застосунку (Streamlit web-інтерфейс)
- Перевіряє назви постів на відповідність правилам
- Підключається до RDS MySQL бази даних
- Запускається на порту 8501

**Основний функціонал:**
- Streamlit інтерфейс для введення даних
- Валідація назв постів
- Збереження результатів в БД
- Відображення статистики

---

## 📂 Папка `setup/` - Початкове налаштування

### `oidc.tf`
**Що робить:**
- Налаштовує OpenID Connect для GitLab
- Дозволяє GitLab отримувати доступ до AWS без статичних ключів
- Створює Trust Relationship для федерації

**Що створюється в AWS:**
- 🔐 **OIDC Provider**: `gitlab.com` 
  - Thumbprint: автоматично генерується
  - Audience: `https://gitlab.com`
- 👤 **IAM Role**: `GitLabOIDCRole` з AdministratorAccess
  - Trust Policy: дозволяє тільки вашому GitLab проекту

**Безпека:**
- GitLab отримує тимчасові облікові дані (на 1 годину)
- Не потрібно зберігати AWS ключі в GitLab
- Доступ обмежений тільки вашим проектом через умову `sub` в Trust Policy

**Файл `new.js`:** містить приклад Trust Policy у JSON форматі

---

### `providers.tf`
**Що робить:**
- Підключає AWS провайдер для Terraform (setup папка)
- Встановлює регіон `us-east-1`

**Що створюється в AWS:** Нічого (це тільки конфігурація)

---

### `variables.tf`
**Що робить:**
- Визначає змінні для setup (регіон AWS)

**Що створюється в AWS:** Нічого (це тільки визначення змінних)

---

## 🔄 Як все працює разом

### 📋 Передумови (що потрібно зробити вручну)

**Перед запуском Terraform:**

1. **Створити S3 бакет для Terraform Backend:**
```bash
aws s3api create-bucket --bucket bucket123ultra --region us-east-1
aws s3api put-bucket-versioning --bucket bucket123ultra --versioning-configuration Status=Enabled
```

2. **Створити DynamoDB таблицю для state locking:**
```bash
aws dynamodb create-table \
  --table-name terraform-lock-table \
  --attribute-definitions AttributeName=LockID,AttributeType=S \
  --key-schema AttributeName=LockID,KeyType=HASH \
  --provisioned-throughput ReadCapacityUnits=5,WriteCapacityUnits=5 \
  --region us-east-1
```

3. **Налаштувати AWS CLI:**
```bash
aws configure
# Ввести: Access Key ID, Secret Access Key, Region (us-east-1)
```

---

### 1️⃣ Початкове розгортання (перший раз)

**Крок 1: Налаштування OIDC для GitLab (один раз)**
```bash
cd setup/
terraform init
terraform plan
terraform apply

# Після успішного apply, скопіюйте ARN ролі для GitLab CI/CD
```

**Крок 2: Створення основної інфраструктури**
```bash
cd ../infra/
terraform init
terraform plan
terraform apply

# Після успішного apply:
# - Скопіюйте ALB URL з outputs
# - Запишіть ECR repository URL
```

**Крок 3: Деплой застосунку**
```bash
# Опція A: Через GitLab CI/CD (рекомендовано)
# Просто запуште код в GitLab - pipeline автоматично запуститься

# Опція B: Вручну
cd infra/app/

# Логін в ECR
aws ecr get-login-password --region us-east-1 | \
  docker login --username AWS --password-stdin 412381736597.dkr.ecr.us-east-1.amazonaws.com

# Білд і push
docker build -t my-app-ecr:latest .
docker tag my-app-ecr:latest 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest
docker push 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest

# Тригер деплою на EC2
aws ssm send-command \
  --document-name "RunTitleCheckerDocker" \
  --parameters imagetag=latest \
  --targets "Key=tag:Name,Values=backend-asg-instance" \
  --region us-east-1
```

**Що відбувається при `terraform apply`:**
```
┌─────────────────────────────────────────────────────────────┐
│                   Порядок створення ресурсів                 │
└─────────────────────────────────────────────────────────────┘

1. 🌐 VPC та мережа (vpc.tf)
   ├── VPC (10.0.0.0/16)
   ├── Internet Gateway
   ├── Public Subnets (2 шт)
   ├── Private Subnets (2 шт)
   ├── NAT Gateways (2 шт)
   └── Route Tables

2. 🛡️ Security Groups (sg.tf)
   ├── ALB Security Group
   ├── EC2 Security Group
   └── RDS Security Group

3. 👤 IAM (iam.tf)
   ├── EC2 Role
   └── Instance Profile

4. ⚖️ Application Load Balancer (alb.tf)
   ├── ALB
   ├── Target Group
   └── Listener

5. 🐳 ECR Repository (ecr.tf)
   └── Docker Registry

6. 🗄️ RDS Database (rds.tf)
   ├── DB Subnet Group
   └── MySQL Instance

7. 📄 SSM Document (ssm.tf)
   ├── Document
   └── Association

8. 🚀 Auto Scaling Group (asg_be.tf)
   ├── Launch Template
   └── Auto Scaling Group

9. ☁️ S3 Upload (s3_upload.tf)
   └── Upload title_checker.py

✅ Infrastructure Ready!
```

**Час створення:** ~10-15 хвилин (найдовше - NAT Gateway та RDS)

---

### 2️⃣ CI/CD Pipeline (автоматичний деплой при кожному push)

**Як працює GitLab CI/CD:**

```
┌──────────────────────────────────────────────────────────────┐
│               GitLab CI/CD Pipeline Flow                      │
└──────────────────────────────────────────────────────────────┘

Developer → git push → GitLab Repository
                           ↓
                    .gitlab-ci.yml
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Stage 1: PLAN (on branch push)                               │
├──────────────────────────────────────────────────────────────┤
│ Job: terraform_plan                                          │
│ • Ініціалізація Terraform                                    │
│ • Валідація конфігурації                                     │
│ • terraform plan (показує що зміниться)                      │
│ • Створює артефакт з планом                                  │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Stage 2: APPLY (on main branch, manual trigger)              │
├──────────────────────────────────────────────────────────────┤
│ Job: terraform_apply                                         │
│ • terraform apply (застосовує зміни)                         │
│ • Оновлює AWS інфраструктуру                                 │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Stage 3: PRE_BUILD                                           │
├──────────────────────────────────────────────────────────────┤
│ Job: pre_build                                               │
│ • Перевіряє чи існує ECR repository                          │
│ • Готує середовище для білду                                 │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Stage 4: BUILD                                               │
├──────────────────────────────────────────────────────────────┤
│ Job: build_and_push                                          │
│ • Логін в AWS ECR                                            │
│ • docker build -t my-app-ecr:latest                          │
│ • Тегує образ з git commit SHA та 'latest'                   │
│ • docker push в ECR                                          │
└──────────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────────┐
│ Stage 5: DEPLOY                                              │
├──────────────────────────────────────────────────────────────┤
│ Job: deploy                                                  │
│ • Знаходить ID EC2 інстансів з тегом backend-asg-instance    │
│ • Відправляє SSM команду на всі EC2                          │
│ • SSM Document запускає:                                     │
│   ├── Оновлення системи                                      │
│   ├── Встановлення Docker                                    │
│   ├── Логін в ECR                                            │
│   ├── Pull нового образу                                     │
│   ├── Зупинка старого контейнера                             │
│   └── Запуск нового контейнера                               │
│ • Чекає completion статус                                    │
└──────────────────────────────────────────────────────────────┘
                           ↓
                    ✅ Деплой успішний!
                           ↓
    Застосунок доступний через ALB URL
```

**Тригери pipeline:**
- 🔄 **Auto:** Кожен push в будь-яку гілку (terraform_plan)
- 🖱️ **Manual:** terraform_apply (тільки в main гілці)
- 🔄 **Auto:** build_and_push після успішного apply
- 🔄 **Auto:** deploy після успішного білду

**Environment variables в GitLab CI/CD:**
```
AWS_ACCOUNT_ID=412381736597
AWS_DEFAULT_REGION=us-east-1
# Якщо використовується OIDC - ключі не потрібні
# Якщо ні:
AWS_ACCESS_KEY_ID=xxxxx
AWS_SECRET_ACCESS_KEY=xxxxx
```

---

### 3️⃣ Доступ до застосунку

**Як користувач отримує доступ:**

```
┌──────────────────────────────────────────────────────────────┐
│                    Traffic Flow Diagram                       │
└──────────────────────────────────────────────────────────────┘

Internet User
    |
    | http://backend-alb-xxx.us-east-1.elb.amazonaws.com
    ↓
┌─────────────────────────────────────────────┐
│     Application Load Balancer (Public)      │
│     Ports: 80 (HTTP), 8501 (Streamlit)     │
│     Health Check: GET / every 30s           │
└─────────────────────────────────────────────┘
    |
    | Security Group: ALB → EC2 (port 8501)
    ↓
┌─────────────────────────────────────────────┐
│    EC2 Instance (Private Subnet)            │
│    ┌─────────────────────────────────────┐ │
│    │  Docker Container                   │ │
│    │  Image: my-app-ecr:latest          │ │
│    │  Port: 8501 (Streamlit app)        │ │
│    │  ┌───────────────────────────────┐ │ │
│    │  │  title_checker.py             │ │ │
│    │  │  (Python Streamlit Web App)   │ │ │
│    │  └───────────────────────────────┘ │ │
│    └─────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
    |
    | Security Group: EC2 → RDS (port 3306)
    ↓
┌─────────────────────────────────────────────┐
│       RDS MySQL Database (Private)          │
│       Port: 3306                            │
│       Engine: MySQL 8.0                     │
│       Storage: Persistent data              │
└─────────────────────────────────────────────┘
```

**Отримати URL застосунку:**
```bash
cd infra/
terraform output load_balancer_url

# Вивід:
# load_balancer_url = "http://backend-alb-1234567890.us-east-1.elb.amazonaws.com"
```

**Доступ на різних портах:**
- `http://alb-url:80` - HTTP (перенаправляється на 8501)
- `http://alb-url:8501` - Прямий доступ до Streamlit

---

### 4️⃣ Оновлення застосунку (після змін коду)

**Автоматичний спосіб (рекомендовано):**
```bash
# 1. Внесіть зміни в код
vim infra/app/title_checker.py

# 2. Commit і push
git add .
git commit -m "Updated application logic"
git push origin main

# 3. Pipeline автоматично:
#    - Побудує новий Docker образ
#    - Запушить в ECR з новим тегом
#    - Задеплоїть на всі EC2 через SSM
#    - Перезапустить контейнери

# 4. Через ~5 хвилин зміни будуть live
```

**Ручний спосіб:**
```bash
# 1. Білд і push образу
cd infra/app/
aws ecr get-login-password --region us-east-1 | \
  docker login --username AWS --password-stdin 412381736597.dkr.ecr.us-east-1.amazonaws.com

docker build -t my-app-ecr:v2.0 .
docker tag my-app-ecr:v2.0 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:v2.0
docker tag my-app-ecr:v2.0 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest
docker push 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:v2.0
docker push 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest

# 2. Тригер деплою
aws ssm send-command \
  --document-name "RunTitleCheckerDocker" \
  --parameters imagetag=v2.0 \
  --targets "Key=tag:Name,Values=backend-asg-instance" \
  --region us-east-1

# 3. Або через Instance Refresh (zero-downtime)
aws autoscaling start-instance-refresh \
  --auto-scaling-group-name backend-asg \
  --preferences MinHealthyPercentage=50 \
  --region us-east-1
```

---

### 5️⃣ Моніторинг та дебаг

**Перевірка здоров'я інфраструктури:**
```bash
# 1. Статус Target Group
aws elbv2 describe-target-health \
  --target-group-arn $(terraform output -raw target_group_arn)

# 2. EC2 інстанси в ASG
aws autoscaling describe-auto-scaling-groups \
  --auto-scaling-group-names backend-asg

# 3. Образи в ECR
aws ecr list-images --repository-name my-app-ecr

# 4. RDS статус
aws rds describe-db-instances \
  --db-instance-identifier $(terraform output -raw db_instance_id)
```

**Підключення до EC2 для дебагу:**
```bash
# Через SSM (без SSH ключів)
# AWS Console → Systems Manager → Session Manager → Start Session
# Вибрати інстанс → Connect

# Або через CLI:
INSTANCE_ID=$(aws ec2 describe-instances \
  --filters "Name=tag:Name,Values=backend-asg-instance" \
  --query "Reservations[0].Instances[0].InstanceId" \
  --output text)

aws ssm start-session --target $INSTANCE_ID

# В сесії:
sudo docker ps
sudo docker logs title_checker
sudo cat /var/log/user-data.log
journalctl -u docker -f
```
    ↓
RDS (якщо потрібна база даних)
```

---

## 🎯 Архітектура (схема)

```
┌─────────────────────────────────────────────────────────┐
│                      INTERNET                            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
            ┌────────────────┐
            │  Route 53 DNS  │ (опціонально)
            └────────┬───────┘
                     │
                     ▼
         ┌───────────────────────┐
         │  Application Load     │  (публічний)
         │  Balancer (ALB)       │  Port 80, 8501
         │  Security Group       │
         └───────────┬───────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
         ▼                       ▼
┌─────────────────┐     ┌─────────────────┐
│  EC2 Instance   │     │  EC2 Instance   │  (приватний)
│  (Docker)       │     │  (Docker)       │  Port 8501
│  Auto Scaling   │     │  Auto Scaling   │
│  Security Group │     │  Security Group │
└────────┬────────┘     └────────┬────────┘
         │                       │
         └───────────┬───────────┘
                     │
                     ▼
            ┌────────────────┐
            │   RDS MySQL    │  (приватний)
            │   Database     │  Port 3306
            │ Security Group │
            └────────────────┘

┌─────────────────────────────────────────────────────────┐
│               AWS Services Used                          │
├─────────────────────────────────────────────────────────┤
│  • VPC - Віртуальна мережа                              │
│  • EC2 - Віртуальні сервери                             │
│  • ECR - Docker Registry                                 │
│  • ALB - Load Balancer                                   │
│  • RDS - База даних                                      │
│  • S3 - Файлове сховище                                  │
│  • SSM - Віддалене управління                            │
│  • IAM - Права доступу                                   │
│  • CloudWatch - Логи та моніторинг                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Команди для роботи

### Розгортання
```bash
cd infra/
terraform init
terraform plan
terraform apply
```

### Отримати URL застосунку
```bash
terraform output load_balancer_url
```

### Підключитися до EC2 через SSM
```bash
# В AWS Console:
Services → Systems Manager → Session Manager → Start Session
```

### Перевірка логів
```bash
# Через SSM на EC2:
sudo cat /var/log/user-data.log
sudo docker logs title_checker
sudo docker ps
```

### Видалення інфраструктури
```bash
terraform destroy
```

---

## 💰 Приблизна вартість

- **VPC**: безкоштовно
- **EC2 t2.micro**: ~$8/місяць (або безкоштовно в Free Tier)
- **ALB**: ~$16/місяць
- **RDS db.t3.micro**: ~$15/місяць
- **NAT Gateway**: ~$32/місяць (найдорожче!)
- **ECR**: $0.10/GB/місяць
- **S3**: майже безкоштовно

**Загалом: ~$70-80/місяць** (без Free Tier)

### Як зменшити витрати:
- Використовуйте 1 NAT Gateway замість 2
- Вимикайте інфраструктуру коли не потрібна
- Використовуйте Spot Instances для EC2

---

## 🔐 Безпека

✅ **Що добре:**
- EC2 в приватних підмережах (без прямого доступу з інтернету)
- RDS тільки доступна для EC2
- SSM замість SSH (безпечніше)
- Security Groups з мінімальними дозволами
- Шифрування в S3 та RDS

⚠️ **Що можна покращити:**
- Додати HTTPS (SSL/TLS) для ALB
- Використовувати AWS Secrets Manager для паролів
- Обмежити доступ до ALB за IP (якщо можливо)
- Увімкнути AWS GuardDuty для виявлення загроз

---

## 📝 Чекліст для деплою

- [ ] AWS CLI налаштований (`aws configure`)
- [ ] Terraform встановлений (`terraform --version`)
- [ ] GitLab CI/CD змінні додані:
  - [ ] `AWS_ACCESS_KEY_ID`
  - [ ] `AWS_SECRET_ACCESS_KEY`
  - [ ] `AWS_ACCOUNT_ID`
- [ ] Dockerfile створений в `infra/app/`
- [ ] Код застосунку в `infra/app/title_checker.py`
- [ ] `terraform.tfvars` налаштований
- [ ] S3 bucket для backend створений
- [ ] DynamoDB table для lock створена
- [ ] Запущено `terraform apply`
- [ ] Перевірено URL від ALB
- [ ] Застосунок працює

---

## 🆘 Troubleshooting

### Проблема: 502 Bad Gateway
**Причини:**
- Контейнер не запущений на EC2
- Health Check не проходить
- Security Group блокує трафік
- Образ не має тегу `latest` в ECR

**Рішення:**
```bash
# 1. Підключитися через SSM (AWS Console → Systems Manager → Session Manager)

# 2. Перевірити статус:
sudo docker ps -a
sudo cat /var/log/user-data.log
sudo systemctl status docker

# 3. Перевірити логи контейнера:
sudo docker logs title_checker

# 4. Перевірити чи образ існує:
aws ecr list-images --repository-name my-app-ecr --region us-east-1
```

### Проблема: Образ не знайдено в ECR
**Симптоми:**
- `docker pull` не знаходить образ з тегом `latest`
- User-data script показує помилку `manifest unknown`

**Рішення:**
```bash
# Опція 1: Перезапустити CI/CD pipeline в GitLab
# Це автоматично побудує і запушить образ

# Опція 2: Вручну побудувати і запушити:
cd infra/app/
aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 412381736597.dkr.ecr.us-east-1.amazonaws.com
docker build -t my-app-ecr:latest .
docker tag my-app-ecr:latest 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest
docker push 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest

# Опція 3: Тригер Instance Refresh в Auto Scaling Group
aws autoscaling start-instance-refresh \
  --auto-scaling-group-name backend-asg \
  --region us-east-1
```

### Проблема: Terraform State Lock
**Симптоми:**
- `Error acquiring the state lock`
- `Lock Info: ID: xxxxx-xxxx-xxxx`

**Причини:**
- Попередній `terraform apply` не завершився коректно
- Два процеси намагаються змінити state одночасно

**Рішення:**
```bash
# Знайти Lock ID в повідомленні помилки, потім:
terraform force-unlock <LOCK_ID>

# Приклад:
terraform force-unlock a1b2c3d4-5678-90ab-cdef-1234567890ab
```

### Проблема: EC2 не реєструється в Target Group
**Причини:**
- Health Check не проходить
- Security Group блокує трафік від ALB
- Контейнер не слухає на порту 8501

**Рішення:**
```bash
# 1. Перевірити Health Check в Target Group (AWS Console):
# EC2 → Target Groups → backend-tg → Targets tab

# 2. Підключитися до EC2 і перевірити:
sudo netstat -tlnp | grep 8501
curl localhost:8501

# 3. Перевірити Security Group:
# Переконатися що EC2 SG дозволяє трафік від ALB SG на порту 8501
```

### Проблема: GitLab CI/CD не може підключитися до AWS
**Симптоми:**
- `Error: error configuring Terraform AWS Provider`
- `NoCredentialProviders: no valid providers in chain`

**Рішення:**
```bash
# Перевірити GitLab CI/CD змінні (Settings → CI/CD → Variables):
# - AWS_ACCOUNT_ID
# - Або AWS_ACCESS_KEY_ID і AWS_SECRET_ACCESS_KEY (якщо не використовується OIDC)

# Перевірити OIDC налаштування:
cd setup/
terraform state list
terraform state show aws_iam_openid_connect_provider.gitlab
```

---

## ❓ FAQ (Часті питання)

### Загальні питання

**Q: Скільки коштує ця інфраструктура?**
A: Приблизно $70-80/місяць без Free Tier. Найдорожче - NAT Gateway ($32/міс). Можна зменшити використовуючи 1 NAT замість 2.

**Q: Чому EC2 в приватних підмережах, а не публічних?**
A: Для безпеки. EC2 недоступний напряму з інтернету, тільки через ALB. Це захищає від прямих атак.

**Q: Навіщо два бакети S3?**
A: `bucket123ultra` - для Terraform state (системний), `newbucktest123456` - для файлів застосунку (створюється Terraform).

**Q: Що таке SSM і навіщо він потрібен?**
A: Systems Manager - сервіс AWS для управління EC2 без SSH. Безпечніше ніж SSH, не потрібні ключі, всі дії логуються.

**Q: Чому використовується OIDC замість AWS ключів?**
A: OIDC видає тимчасові облікові дані (на 1 год), безпечніше ніж зберігати статичні ключі в GitLab. Якщо GitLab злам ають - ключі вже невалідні.

**Q: Що станеться якщо видалити NAT Gateway?**
A: EC2 не зможе завантажувати Docker образи з ECR та оновлення. Але для тестування можна використовувати VPC Endpoints замість NAT.

### Про деплой

**Q: Як швидко відбувається деплой?**
A: 
- Terraform apply: 10-15 хв (перший раз)
- Docker build + push: 2-3 хв
- SSM deploy на EC2: 1-2 хв
- **Загалом:** ~5 хв для код апдейту, ~15 хв для інфра змін

**Q: Чи буде downtime при оновленні?**
A: 
- При оновленні через SSM - так, ~30 сек (поки контейнер перезапускається)
- Для zero-downtime використовуйте Instance Refresh з MinHealthyPercentage=50

**Q: Як відкатити до попередньої версії?**
A:
```bash
# Деплой конкретної версії образу
aws ssm send-command \
  --document-name "RunTitleCheckerDocker" \
  --parameters imagetag=v1.0 \
  --targets "Key=tag:Name,Values=backend-asg-instance"
```

**Q: Чи можна деплоїти без GitLab CI/CD?**
A: Так, вручну через AWS CLI (команди наведені в секції "Оновлення застосунку").

### Про масштабування

**Q: Як додати більше EC2 інстансів?**
A: Змініть в `asg_be.tf`:
```hcl
min_size         = 2  # було 1
max_size         = 4  # було 1
desired_capacity = 2  # було 1
```

**Q: Чи можна використовувати більший тип EC2?**
A: Так, змініть `instance_type` в `variables.tf`:
```hcl
variable "instance_type" {
  default = "t2.small"  # було t2.micro
}
```

**Q: Як налаштувати автоматичне масштабування по CPU?**
A: Додайте в `asg_be.tf`:
```hcl
resource "aws_autoscaling_policy" "scale_up" {
  name                   = "scale-up"
  scaling_adjustment     = 1
  adjustment_type        = "ChangeInCapacity"
  autoscaling_group_name = aws_autoscaling_group.backend_asg.name
}

resource "aws_cloudwatch_metric_alarm" "high_cpu" {
  alarm_name          = "high-cpu"
  comparison_operator = "GreaterThanThreshold"
  evaluation_periods  = "2"
  metric_name         = "CPUUtilization"
  namespace           = "AWS/EC2"
  period              = "120"
  statistic           = "Average"
  threshold           = "80"
  alarm_actions       = [aws_autoscaling_policy.scale_up.arn]
}
```

### Про безпеку

**Q: Чи безпечно зберігати паролі в terraform.tfvars?**
A: Ні! Використовуйте AWS Secrets Manager або змінні оточення. Додайте `terraform.tfvars` в `.gitignore`.

**Q: Як додати HTTPS?**
A:
1. Отримайте SSL сертифікат в AWS Certificate Manager (безкоштовно)
2. Додайте listener на порт 443 в `alb.tf`
3. Перенаправляйте HTTP на HTTPS

**Q: Як обмежити доступ до застосунку за IP?**
A: Додайте в ALB Security Group:
```hcl
ingress {
  from_port   = 80
  to_port     = 80
  protocol    = "tcp"
  cidr_blocks = ["1.2.3.4/32"]  # тільки ваша IP
}
```

### Про troubleshooting

**Q: 502 Bad Gateway - що робити?**
A: Див. секцію "Troubleshooting" → "Проблема: 502 Bad Gateway"

**Q: Як подивитися логи застосунку?**
A:
```bash
# Через SSM Session Manager:
sudo docker logs title_checker
sudo docker logs title_checker -f  # follow mode
```

**Q: Terraform state locked - як розблокувати?**
A:
```bash
terraform force-unlock <LOCK_ID>
# LOCK_ID показується в повідомленні про помилку
```

**Q: EC2 не може завантажити образ з ECR?**
A: Перевірте:
1. Чи є образ з тегом `latest` в ECR
2. Чи EC2 має IAM роль з ECR дозволами
3. Чи працює NAT Gateway (для приватних підмереж)

---

## 📖 Глосарій термінів та абревіатур

### AWS Сервіси

| Термін | Повна назва | Що це таке |
|--------|-------------|------------|
| **VPC** | Virtual Private Cloud | Ізольована віртуальна мережа в AWS. Як власний дата-центр в хмарі |
| **EC2** | Elastic Compute Cloud | Віртуальні сервери (комп'ютери) в AWS |
| **ALB** | Application Load Balancer | Розподіляє вхідний трафік між кількома серверами |
| **ASG** | Auto Scaling Group | Автоматично додає/видаляє EC2 інстанси залежно від навантаження |
| **RDS** | Relational Database Service | Керована база даних (MySQL, PostgreSQL тощо) |
| **ECR** | Elastic Container Registry | Приватне сховище Docker-образів (як Docker Hub, але приватний) |
| **SSM** | Systems Manager | Інструмент для управління EC2 без SSH |
| **IAM** | Identity and Access Management | Управління правами доступу в AWS |
| **S3** | Simple Storage Service | Сховище файлів в хмарі (як Dropbox для серверів) |
| **NAT** | Network Address Translation | Gateway що дозволяє приватним серверам виходити в інтернет |
| **SG** | Security Group | Віртуальний фаєрвол для контролю трафіку |
| **AZ** | Availability Zone | Фізичний дата-центр AWS в регіоні |
| **CIDR** | Classless Inter-Domain Routing | Формат для опису IP-адрес (напр. 10.0.0.0/16) |
| **OIDC** | OpenID Connect | Протокол аутентифікації без паролів |

### Terraform терміни

| Термін | Що це |
|--------|-------|
| **State** | Файл що зберігає інформацію про створені ресурси |
| **Backend** | Місце зберігання state файлу (S3, локально тощо) |
| **Provider** | Плагін для роботи з хмарою (AWS, Azure, GCP) |
| **Resource** | Окремий компонент інфраструктури (EC2, VPC тощо) |
| **Module** | Набір ресурсів згрупованих разом для переіспользування |
| **Variable** | Змінна що можна передати в конфігурацію |
| **Output** | Значення що виводиться після створення (URL, ID тощо) |
| **Plan** | Попередній перегляд змін перед застосуванням |
| **Apply** | Застосування змін до інфраструктури |
| **Destroy** | Видалення всієї інфраструктури |

### Docker терміни

| Термін | Що це |
|--------|-------|
| **Image** | Шаблон для створення контейнера (як ISO образ) |
| **Container** | Запущений екземпляр image (як запущена програма) |
| **Dockerfile** | Рецепт для створення image |
| **Registry** | Сховище для Docker images (ECR, Docker Hub) |
| **Tag** | Версія образу (latest, v1.0, production) |
| **Port mapping** | Прокидання порту з контейнера назовні (-p 8501:8501) |

### Мережеві терміни

| Термін | Що це |
|--------|-------|
| **Subnet** | Підмережа - частина великої мережі |
| **Public Subnet** | Підмережа з прямим доступом в інтернет |
| **Private Subnet** | Підмережа без прямого доступу в інтернет |
| **Internet Gateway** | Шлюз для доступу в інтернет |
| **Route Table** | Таблиця маршрутизації (куди направляти трафік) |
| **Health Check** | Перевірка чи сервер живий та працює |
| **Target Group** | Група серверів за якими розподіляється трафік |
| **Ingress** | Вхідний трафік (з інтернету до сервера) |
| **Egress** | Вихідний трафік (з сервера в інтернет) |

### CI/CD терміни

| Термін | Що це |
|--------|-------|
| **Pipeline** | Автоматизований процес від коду до деплою |
| **Stage** | Етап в pipeline (build, test, deploy) |
| **Job** | Конкретна задача в stage |
| **Artifact** | Файл створений в job (образ, бінарник) |
| **Trigger** | Умова що запускає pipeline (push, schedule) |
| **Runner** | Сервер що виконує jobs |

---

## 📚 Додаткові ресурси

### 📖 Офіційна документація AWS:
- [AWS VPC Documentation](https://docs.aws.amazon.com/vpc/) - Налаштування віртуальних мереж
- [AWS ALB Documentation](https://docs.aws.amazon.com/elasticloadbalancing/) - Application Load Balancer
- [AWS Systems Manager](https://docs.aws.amazon.com/systems-manager/) - SSM для управління EC2
- [AWS ECR Documentation](https://docs.aws.amazon.com/ecr/) - Container Registry
- [AWS Auto Scaling](https://docs.aws.amazon.com/autoscaling/) - Автоматичне масштабування

### 🔧 Terraform:
- [Terraform AWS Provider](https://registry.terraform.io/providers/hashicorp/aws/latest/docs) - Всі AWS ресурси
- [Terraform Best Practices](https://www.terraform-best-practices.com/) - Найкращі практики
- [Terraform Modules Registry](https://registry.terraform.io/) - Готові модулі

### 🐳 Docker & Containers:
- [Docker Best Practices](https://docs.docker.com/develop/dev-best-practices/) - Оптимізація образів
- [Streamlit Documentation](https://docs.streamlit.io/) - Для вашого застосунку

### 🔐 GitLab CI/CD:
- [GitLab CI/CD Documentation](https://docs.gitlab.com/ee/ci/) - Pipeline конфігурація
- [GitLab OIDC with AWS](https://docs.gitlab.com/ee/ci/cloud_services/aws/) - Федерація без ключів

---

## 🎓 Корисні команди

### Terraform
```bash
# Ініціалізація (перший раз або після змін backend)
terraform init

# Перевірка синтаксису
terraform validate

# Форматування коду
terraform fmt -recursive

# Планування змін
terraform plan -out=tfplan

# Застосування змін
terraform apply tfplan

# Перегляд поточного стану
terraform state list
terraform state show aws_instance.example

# Видалення інфраструктури
terraform destroy

# Імпорт існуючих ресурсів
terraform import aws_instance.example i-1234567890abcdef0
```

### AWS CLI
```bash
# Перевірка облікових даних
aws sts get-caller-identity

# Перегляд EC2 інстансів
aws ec2 describe-instances --filters "Name=tag:Name,Values=backend-asg-instance"

# Перегляд образів в ECR
aws ecr list-images --repository-name my-app-ecr

# Підключення до EC2 через SSM
aws ssm start-session --target i-xxxxxxxxx

# Перегляд логів CloudWatch
aws logs tail /aws/ssm/RunTitleCheckerDocker --follow

# Тригер Instance Refresh
aws autoscaling start-instance-refresh --auto-scaling-group-name backend-asg
```

### Docker
```bash
# Логін в ECR
aws ecr get-login-password --region us-east-1 | \
  docker login --username AWS --password-stdin 412381736597.dkr.ecr.us-east-1.amazonaws.com

# Білд образу
docker build -t my-app-ecr:latest .

# Тегування
docker tag my-app-ecr:latest 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest

# Push в ECR
docker push 412381736597.dkr.ecr.us-east-1.amazonaws.com/my-app-ecr:latest

# Локальний запуск для тестування
docker run -p 8501:8501 my-app-ecr:latest
```

---

## ✅ Checklist: Готовність до production

### Безпека
- [ ] HTTPS/SSL сертифікат для ALB (через ACM)
- [ ] Секрети в AWS Secrets Manager (не в коді!)
- [ ] MFA для AWS Root аккаунту
- [ ] Обмеження доступу по IP для критичних ресурсів
- [ ] Увімкнено AWS CloudTrail для аудиту
- [ ] Регулярне сканування Docker образів на вразливості

### Моніторинг та логування
- [ ] CloudWatch Alarms для критичних метрик
- [ ] CloudWatch Logs для всіх компонентів
- [ ] Dashboard в CloudWatch
- [ ] SNS для алертів (email/Slack)
- [ ] Налаштовано retention policy для логів

### Резервне копіювання
- [ ] RDS Automated Backups увімкнено
- [ ] Snapshots EC2 AMI для швидкого відновлення
- [ ] S3 Versioning для критичних файлів
- [ ] Terraform state зберігається в S3 з versioning

### Високодоступність
- [ ] Multi-AZ для RDS
- [ ] Мінімум 2 AZ для ALB та ASG
- [ ] Health checks налаштовані коректно
- [ ] Auto Scaling policies для навантаження

### Вартість оптимізація
- [ ] Перевірено розміри інстансів (не overprovisioning)
- [ ] Використовується Savings Plans / Reserved Instances
- [ ] Налаштовано AWS Budgets для контролю витрат
- [ ] CloudWatch Logs retention не нескінченний

---

**Автор:** DevOps Team  
**Останнє оновлення:** Грудень 2024  
**Версія:** 1.1
